# Skill template

SKILL.md uses paths relative to the generated skill package. Generate that
package from the repository root with:

```sh
python package_skill.py --out build/skill-package
```

The output contains SKILL.md and its self-contained assets/audio-tools tree.
Use that generated directory when installing the TS2068 audio skill.
